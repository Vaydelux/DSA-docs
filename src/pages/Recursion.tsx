import Placeholder from '../components/Placeholder';
export default function Recursion() {
  return <Placeholder title="Recursion" description="Base cases, recursive cases, call stack, and how to think recursively." />;
}
