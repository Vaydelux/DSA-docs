import Placeholder from '../components/Placeholder';
export default function Arrays() {
  return <Placeholder title="Arrays & Strings" description="Contiguous memory, O(1) access, two pointers, sliding window, and prefix sums." />;
}
